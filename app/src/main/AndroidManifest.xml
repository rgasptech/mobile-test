<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.victorpettengill.rgacodechallenge">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".application.RGACodeChallengeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.ContactListActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.ContactSaveActivity"
            android:label="@string/title_activity_contact_save"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.ContactDetailsActivity"
            android:label="@string/title_activity_contact_details"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>