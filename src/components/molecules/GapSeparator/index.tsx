import React from 'react';
import {Gap} from '~components/atoms';
import {spaces} from '~constants/spaces';

const GapSeparator = () => <Gap vertical={spaces.medium} />;

export default GapSeparator;
